import React from 'react';
import './index.css';

const Toolbar = props => (
    <header className =  'Toolbar'>
        <div>MENU</div>
        <div>LOGO</div>
        <nav>
            ...
        </nav>
    </header>
);

export default Toolbar;